# Leave these blank to run in local demo mode (data persists in the browser).
# Fill them in to connect to the AWS backend (see backend/ + infra/aws-setup.md).
VITE_API_URL=
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
